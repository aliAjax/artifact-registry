{
  "sample_id": 207,
  "session_id": "",
  "bug_id": "ar-concurrency-drain-replication-007",
  "task_type": "bugfix",
  "bug_category": "concurrency并发问题",
  "repo_url": "",
  "go_version": "golang:1.23; go 1.23.0; GOTOOLCHAIN=local",
  "repro_determinism": "deterministic",
  "user_query": "复制协调器关掉之后还能往里塞任务直接 panic，Drain 数出来的任务数也不对，队列 Pop 之后再 Push 同一个 ID 就进不去了。帮我修好。",
  "trajectory": "",
  "verify_cmds": "go test -race ./internal/replicationctl -run '^TestEnqueueAfterCloseErrors$' -count=1\ngo test -race ./internal/replicationctl -run '^TestDrainCountsTasks$' -count=1\ngo test -race ./internal/replicationctl -run '^TestQueuePopAllowsRepush$' -count=1\ngo test -race ./internal/replicationctl -run '^TestQueueLen$' -count=1\ngo test -race ./internal/replicationctl -run '^TestCoordinatorConcurrentDrain$' -count=1",
  "gold_root_cause": "文件: internal/replicationctl/coordinator.go, internal/replicationctl/queue.go 符号: Coordinator.Enqueue, Coordinator.Drain, Queue.Pop, Queue.Len 机制: 关闭后仍向已关闭 channel 发送导致 panic，Pop 不清理去重标记，Drain/Len 计数错位，channel 生命周期与去重状态不同步",
  "gold_patch": "",
  "success_criteria": "1. Close 后 Enqueue 要返回错误而非 panic，Drain 要数对任务数，Pop 后能再 Push 同 ID，Len 要准确；2. 修完这五条连续 20 遍全绿；3. 回退任一修复，对应测试又变红；4. 全量测试零回归，只验证公开行为。",
  "verify_result": "",
  "harness": "",
  "generator_model": "",
  "worker": "",
  "creator": "",
  "qc_result": "",
  "qc_note": "",
  "sync_feishu": "",
  "coverage_contract": {
    "version": 1,
    "claims": [
      {
        "id": "enqueue_after_close_errors",
        "description": "Close 后 Enqueue 返回错误",
        "test": "TestEnqueueAfterCloseErrors",
        "command": "go test -race ./internal/replicationctl -run '^TestEnqueueAfterCloseErrors$' -count=1",
        "paths": [
          "internal/replicationctl/coordinator.go"
        ]
      },
      {
        "id": "drain_counts_tasks",
        "description": "Drain 数对任务数",
        "test": "TestDrainCountsTasks",
        "command": "go test -race ./internal/replicationctl -run '^TestDrainCountsTasks$' -count=1",
        "paths": [
          "internal/replicationctl/coordinator.go"
        ]
      },
      {
        "id": "queue_pop_repush",
        "description": "Pop 后可再 Push 同 ID",
        "test": "TestQueuePopAllowsRepush",
        "command": "go test -race ./internal/replicationctl -run '^TestQueuePopAllowsRepush$' -count=1",
        "paths": [
          "internal/replicationctl/queue.go"
        ]
      },
      {
        "id": "queue_len",
        "description": "Queue.Len 准确",
        "test": "TestQueueLen",
        "command": "go test -race ./internal/replicationctl -run '^TestQueueLen$' -count=1",
        "paths": [
          "internal/replicationctl/queue.go"
        ]
      },
      {
        "id": "coordinator_concurrent_drain",
        "description": "并发 Drain 不报竞态",
        "test": "TestCoordinatorConcurrentDrain",
        "command": "go test -race ./internal/replicationctl -run '^TestCoordinatorConcurrentDrain$' -count=1",
        "paths": [
          "internal/replicationctl/coordinator.go"
        ]
      }
    ]
  }
}